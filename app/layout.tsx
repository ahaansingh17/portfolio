import type { Metadata } from "next";
import { Inter, JetBrains_Mono } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

const jetbrains = JetBrains_Mono({
  subsets: ["latin"],
  variable: "--font-jetbrains",
});

export const metadata: Metadata = {
  title: "Ahaan Singh — AI & Data Professional",
  description:
    "Ahaan Singh — AI engineer, builder and community leader, and published researcher on AI fairness. New Delhi, India.",
  openGraph: {
    title: "Ahaan Singh — AI & Data Professional",
    description:
      "I build AI systems, lead communities, and turn ideas into impact.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en" className={`${inter.variable} ${jetbrains.variable}`}>
      <body className="font-sans">{children}</body>
    </html>
  );
}
